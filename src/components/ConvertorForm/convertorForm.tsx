import React, {FunctionComponent} from 'react';
import {ICash} from "../../types/types";

interface convertorFormProps {
     values: ICash[],
}

const ConvertorForm:FunctionComponent<convertorFormProps> = ({children}) => {
    return (
        <div>

        </div>
    );
};

export default ConvertorForm;